{
  "name": "shadowking-free-factory",
  "version": "4.0.0",
  "description": "مصنع الملك بالبايثون",
  "scripts": {
    "start": "pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 10000"
  },
  "dependencies": {},
  "engines": {
    "python": "3.10.x"
  }
}