services:
    - type: web
    name: ai-proxy-qnen
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
            - key: GITHUB_TOKEN
        value: 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